<div class="book">
	<div class="book-image">
		<img src={src} alt={`${title} by ${author}`} width={width} height={height} />
	</div>
	<div class="description">
		<h2><a href={link} target="_blank" rel="noreferrer" title="Buy on bookshop.org"></a>
			<p class="meta">by {author}</p>
			<p class="thoughts">
				<slot />
			</p>
	</div>
</div>
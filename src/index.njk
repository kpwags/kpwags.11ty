---
title: ''
layout: 'layouts/layout.html'
---

<section class="intro homepage-section ">
	<div class="image"><img src="/img/logo.png" alt="A picture of me in a Phillies shirt and hat holding a beer" width="128" height="128" /></div>
	<div class="text">
		<div class="hi-there">Hi, I'm Keith!</div>
		<div class="basic-info">I'm a software developer, gamer, geek, beer league hockey player, Philadelphia sports fan and a whole lot more.</div>
	</div>
</section>

<section class="homepage-section">
	<div class="title">
		<h2>Pinned Posts</h2>
	</div>

	{% set pinnedPostsSorted = collections.pinnedPosts | reverse %}
	{% set pinnedPosts = pinnedPostsSorted.slice(0, 5) %}

	<ul>
		{% for post in pinnedPosts %}
			<li>
				<a href="{{ post.url }}">
					<span class="text">{{ post.data.title }}</span>
					<span class="meta">{{ post.data.dateString }}</span>
				</a>
			</li>
		{% endfor %}
	</ul>
</section>

<section class="homepage-section">
	<div class="title">
		<h2>Latest Posts</h2>
		<a href="/blog">View More</a>
	</div>

	{% set recentPostsSorted = collections.publicPosts | reverse %}
	{% set recentPosts = recentPostsSorted.slice(0, 5) %}

	<ul>
		{% for post in recentPosts %}
			<li>
				<a href="{{ post.url }}">
					<span class="text">{{ post.data.title }}</span>
					<span class="meta">{{ post.data.dateString }}</span>
				</a>
			</li>
		{% endfor %}
	</ul>
</section>

<section class="homepage-section">
	<div class="title">
		<h2>Latest Reading Logs</h2>
		<a href="/reading-logs">View More</a>
	</div>

	{% set recentLogsSorted = collections.readinglog | reverse %}
	{% set recentLogs = recentLogsSorted.slice(0, 3) %}

	<ul>
		{% for post in recentLogs %}
			<li>
				<a href="{{ post.url }}">
					<span class="text">{{ post.data.title }}</span>
					<span class="meta">{{ post.data.dateString }}</span>
				</a>
			</li>
		{% endfor %}
	</ul>
</section>

<section class="homepage-section">
	<div class="title">
		<h2>Latest Notes</h2>
		<a href="/notes">View More</a>
	</div>

	{% set recentNotesSorted = collections.shortnotes | reverse %}
	{% set recentNotes = recentNotesSorted.slice(0, 5) %}

	<ul>
		{% for note in recentNotes %}
			<li>
				<a href="{{ note.url }}">
					<span class="text">{{ note.data.title }}</span>
					<span class="meta">{{ note.data.dateString }} &bull; {{ note.data.link | domainFromUrl }}</span>
				</a>
			</li>
		{% endfor %}
	</ul>
</section>

<section class="homepage-section">
	<div class="title">
		<h2>Latest Book Note</h2>
		<a href="/book-notes">View More</a>
	</div>

	{% set recentBookNotesSorted = collections.booknotes | reverse %}
	{% set bookNote = recentBookNotesSorted[0] %}

	<ul>
		<li>
			<a href="{{ bookNote.url }}">
				<span class="text">{{ bookNote.data.title }}</span>
				<span class="meta">{{ bookNote.data.dateString }} &bull; {{ bookNote.data.author }}</span>
			</a>
		</li>
	</ul>
</section>

<section class="homepage-section latest-pixelfed-photos">
	<div class="title">
		<h2>Latest from pixelfed</h2>
		<a href="https://pixelfed.social/users/kpwags">View More</a>
	</div>

	<div class="pixelfed-photos">
		{% for img in pixelfed %}
			<a href="{{ img.link }}">
				<div class="image" title="{{ img.title }}" style="background-image: url('{{ img.image }}')"></div>
			</a>
		{% endfor %}
	</div>
</section>

<section class="homepage-section">
	<div class="title">
		<h2>Latest Links</h2>
		<a href="/links">View More</a>
	</div>

	{% set recentLinksSorted = links %}
	{% set recentLinks = recentLinksSorted.slice(0, 5) %}

	<ul>
		{% for link in recentLinks %}
			<li>
				<a href="{{ link.link }}">
					<span class="text">{{ link.title }}</span>
					<span class="meta">{{ link.linkDate | readableDate }} &bull; {{ link.author }}</span>
				</a>
			</li>
		{% endfor %}
	</ul>
</section>